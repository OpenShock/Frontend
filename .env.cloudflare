# Config for the official OpenShock deployment on Cloudflare Pages ONLY.
# Loaded exclusively when building with `--mode cloudflare` (see `pnpm build:cloudflare`,
# which the Cloudflare Pages build command must use). Docker/self-host builds run in
# `production` mode and never load this file — configure those via docker-compose env vars.

PUBLIC_SITE_URL=https://openshock.app
PUBLIC_SITE_SHORT_URL=https://openshock.app
PUBLIC_BACKEND_API_URL=https://api.openshock.app
PUBLIC_GATEWAY_CSP_WILDCARD=https://*.openshock.app

# Ship browser logs/errors to SigNoz — official deployment only.
PUBLIC_SIGNOZ_LOGS_ENABLED=true
PUBLIC_SIGNOZ_LOGS_URL=https://signoz.openshock.app/v1/logs
PUBLIC_SIGNOZ_TRACES_URL=https://signoz.openshock.app/v1/traces
PUBLIC_SIGNOZ_TRACE_PROPAGATION=true
PUBLIC_SIGNOZ_DEPLOYMENT_ENVIRONMENT=production
PUBLIC_SIGNOZ_RESOURCE_ATTRIBUTES=
